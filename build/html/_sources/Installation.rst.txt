Install NodeJs
==============

ติดตั้ง ใช้ nvm  

.. code-block:: bash

    curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.34.0/install.sh | bash
    nvm ls-remote
    nvm install node
    nvm ls
    node --version

